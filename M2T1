// CSC 134
// M2T1 - Product Sales
// Jordan Bell
// 2/8/26
// We're going to make the simplest possible
// "checkout" machine
#include <iostream>
#include <iomanip> // for the 2 decimal places
using namespace std;


int main() {

    //Set up all variables
    string Jordan, Bell, full_name; //holds costumer name
    string product = "Cheeseburgers"; //change to whatever you like
    int amount_purchased;
    double cost_each = 4.99;
    double total_cost;


    // Greet the customer
    cout << "Welcome to the " << product << " store!" << endl;
    cout << "What is your first name? ";
    cin >> Jordan;
    cout << "What is your last name? ";
    cin >> Bell;
    full_name = Jordan + " " + Bell;
    cout << "Nice to meet you, " << full_name << endl;

    // Ask how much they'd like to purchase
    cout << "How many " << product << " would you like today? ";
    cin >> amount_purchased;

    //Calculate the total price
    total_cost = amount_purchased * cost_each;

    // Formatting: set all prices to 2 decimal places
    cout << setprecision(2) << fixed;

    //Give the result
    cout << "For " << amount_purchased << " " << product << endl;
    cout << "That will be: $" << total_cost << endl;
    cout << "Thank you for shopping with us!" << endl;

    return 0; //no errors
}
